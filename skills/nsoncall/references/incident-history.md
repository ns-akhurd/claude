# DLP Oncall Incident History (full ledger)

Source: `/root/code/tools/dlp_oncall_tracker/dlp_oncall.db`, `messages` table, id 1-2399, spanning 2025-04 through 2026-07-05, plus 10 live `/nsoncall` triage sessions 2026-07-04 to 2026-07-08 (the DB's Slack fetch stalled at id 2399 on 2026-07-05 — `invalid_refresh_token`; triage sessions are the only source of learnings past that date until re-authenticated). Mined in 5 slices by parallel agents. This is the raw per-incident detail behind the condensed playbook in `../SKILL.md` — use it when the playbook pattern doesn't match closely enough and you need the original ticket/host/tenant specifics. 160 entries total (15 + 55 + 12 + 51 + 27).

Categories: FP Blockers, WD Aborts, DP Issues, MP Issues, Scan Failures, Tenant Issues, Integration Failures, Other.

---

## Slice 1 (id 1-504, ~2026-03 to 2026-04-11) — 15 entries

| Category | Severity | Plane | Status | Summary | Jira | Action Taken |
|---|---|---|---|---|---|---|
| FP Blockers | Critical | MP | Resolved | FP1.0 singleton race ("Classification[N] is not found in hash" / "Fingerprint classification failed for fc_id=N") caused all scans for the affected tenant on a host to fail; hit tenants 8602, 2831, 12809, 13446, 11804 across ord2/sv5/sjc1/dxb1, ~4-5 machines/day at peak in SV5. | ENG-933714, CM-212704 | Root cause: `FingerprintEngineFactory::instance()` non-threadsafe init reused by inotify code so FP config never loads for the engine. Runbook: drain host, `sudo supervisorctl restart dlpsvc:nsdlp_0`, re-add to pool, log in CM-212704. Permanent fix merged to R135/R136. |
| WD Aborts | High | MP | Mitigated | WD aborts continued even after R135.1; a WD abort directly preceded the FP1.0 hash bug on the same host (tenant 8602, 2026-03-17). | ENG-933714 | Manually enabled EDK-chunking feature flag per problematic tenant on SIN2 hosts. |
| WD Aborts | High | MP | Resolved | Tenant 4673 (Airbus) drove repeated WD aborts in FRA1; separately took down all 3 FRA1 DLP servers and pushed AM2 to ~100% CPU via a new profile ("National Restricted - Malaysia Restricted") with questionable regexes. | ENG-477599, CM-217443 | Isolated tenant to 1 dedicated FRA1 server; throttled introspection traffic on AM2; engaged regex review. |
| Scan Failures | Medium | DP | Resolved | Proxy-side `dlpmod_error_type: OTHER` metric spikes (BOM2) — emitted by NSProxy, not DLP. | ENG-911050 | Distinguished from `RESP_TIMEOUT`/`RESP_FAILURE`; added detail to ticket. |
| Scan Failures | Low | Unknown | Resolved | Tenant 19555 `.snp` text-extraction failures (`filetype=Unknown_Fmt`); tenant 22744 similar profile-load issue. | ENG-860174 | Confirmed as previously-fixed issue class; resurfaced later without further diagnosis. |
| MP Issues | Critical | MP | Resolved | MEL2 haproxy VIP failure: both haproxy VMs failed, traffic to MEL2 stopped entirely. | EIMF-316, OPS-259632 | Restarted `keepalived`; haproxy min core/memory sizing reviewed for downsized DCs. |
| DP Issues | Medium | DP | Resolved | DUB1: iaasmpdlp servers running old R134.1 build (same as SJC2). | CD-58504, CD-58505 | CD ticket to deploy R135 and RCA the stale build. |
| Scan Failures / Tenant Issues | Medium | MP | Recurring | "Overlap limit 128 exceeded" CI errors from overly broad regex profiles: tenant 6133, tenant 26443 TPE1, tenant 2813 Alkami (>50% scan failures), tenant 1329 WWT (raw `.*` regex). | CS-4290 | File CX/CS ticket asking customer to remove/tighten regex; discussed "dedup flag" mitigation. |
| Tenant Issues | Medium | MP | Open | Tenants exceeding `client_timeout`: tenant 13446 NRT3, tenant 19494 SIN2, tenant 22425 SIN2. | ENG-729021, CS-4244 | Recommended tenant/smtp client raise `client_timeout` above default 10s. |
| Integration Failures | Critical | MP | Escalated | MP DLP DRM CURL timeouts BLOCKER (>=1000) in CH-ZUR2, plus SYD2/SJC1 DRM alerts. | none | Forwarded to DRM team / #dlp-drm. |
| MP Issues | Critical | MP | Mitigated | R136 Day-1 rollout SIGABRT crash wave (`DlpRedisClient::loadEntry` → `Json::Value::asString` throw, `EvtUtils.cpp:171`) on DEL1/DXB1, traced to new "priority-based eviction of incidents" feature. | ENG-965176, CM-224056 | Disabled `enable-evict-incidents`/`enable-evict-forensics` on DEL1; considered rollback. |
| MP Issues | High | MP | Investigating | `nsdlpcachesvc` pods crashing in prod, possibly same cache bug as above. | ENG-970341, ENG-969470 | Requested pod restarts. |
| MP Issues | Medium | MP | Mitigated | `dlppool1-6.mad2`/`dlppool1-10.bcn1` missed R136 rollout, stayed on old dplib, kept crashing (same EvtUtils.cpp:171). | ENG-969470, OPS-263369, ART-1026337 | Maintenance mode; engaged CD/PE on why automation missed the hosts. |
| Integration Failures | Low | Unknown | Open | Vendor (KeyView/Qosmos) DLL crash investigation; required `LD_DEBUG` env vars + `format.ini` change on a CCU1 prod server. | CM-225985 | Required prior approval (Emanoel/Niranjan/Abhiram) before any prod change. |
| Other | High | Unknown | Mitigated | Ceph OSD node stuck / Mon quorum loss affecting DLP-adjacent storage. | IMF-1332 | Restarted the stuck OSD node. |

**Recurring alerts (slice 1):** FP1.0 hash-error blocker (20+ firings, Mar 10-Apr 11); scan-failure-% blocker by host/tenant (5+); proxy scan-response-timeout ≥1% (1, no runbook existed); DP DLP DRM CURL timeouts ≥1000 (2).

**Sumo/query artifacts (slice 1):** No literal Sumo operator syntax found in this slice — closest artifact was a Grafana/PromQL dashboard query for `dlpmod_error_type` (not Sumo).

---

## Slice 2 (id 505-1008, ~2026-04 to 2026-06-21) — 55 entries

| Category | Severity | Plane | Status | Summary | Jira | Action Taken |
|---|---|---|---|---|---|---|
| MP Issues | high | MP | resolved | Repeated tenant crashes traced to HS/CH (Hyperscan/compiled-hyperscan) regex compilation; 4th-5th tenant hit by same issue. | CM-263723 | Disabled HS/CH regex compilation in commercial MPs; runbook requested. |
| WD Aborts | critical | DP | resolved | FRA1: tenant 4673 (AirBus) watchdog aborts killed all 3 servers; DP recovered on its own. | none | None explicit (later EDK chunking considered for AirBus). |
| DP Issues | critical/P1 | DP | resolved | BOG1 "DP DLP servers down (haproxy logs)" — `dlp_be`/`dlp_rest_be` pools hit `min_servers=1`. | none | Temporary fix applied; P1 paged to ENG-DLP. |
| Other | low | unknown | unclear | "CRITICAL: Event Generator Errors Report" mostly `"BENIGN: tenant load race"` — unclear if action needed. | none | none |
| Integration Failures | high | MP | open | SJC2 mpdlp servers unable to reach EG (`telnet dlp-event-generator 80` connection refused; ping OK); 720,928 "Could not connect to server" errors in 60 min; EG posts to IMS failing/retrying. | OPS-278003 | Retries continue; no fix confirmed in this slice. |
| DP Issues | low | DP | open | `dlppool1-7.osa2.nskope.net` found running old build 137.0.0.3736. | OPS-277665 | Plan to disable host in haproxy if issues arise. |
| Scan Failures | critical | DP | open | `CRITICAL DP DLP Scans Timeout By (Pop,Tenant) Over Incoming > 5%` in us-west-2, currently 7.84%; CPU/memory graphs unavailable to diagnose. | none | none |
| Integration Failures | high | unknown | resolved | CM-263027 caused a DNS issue. | CM-263027 | CM changes rolled back; confirmed working. |
| DP Issues | high | DP | open | SJC1 near-outage; root-cause writeup: DRM `status=0` metric conflates `CS_TIMEDOUT`+`CS_ERROR`; DRM circuit breaker (`DrmComponent::applicable`) commented out since R108, so DP keeps sending every file at full 5-30s timeout with no backpressure when DRM is slow. | none | Recommended: forward curl res/errStr, split metric tag, re-enable circuit breaker (not yet implemented). |
| Scan Failures | high | DP | open | EIMF-501 (SCL2): tenant 24727 hit `DLP Profile Load Failures Per Tenant > 10` (currently 37); SCL2 CPU pinned at 100%, can't close IMF until capacity provisioned. | EIMF-501 | Pending capacity provisioning. |
| DP Issues | high | DP | open | dlp-async k8s pods (SJC2, then WAW1) evicted — no ephemeral-storage requests/limits configured on containers, triggers eviction under node storage pressure. | none | Fix (set ephemeral-storage limits) identified but not yet applied. |
| Tenant Issues | high | DP | open | Tenant 14714 (LON2): policy referenced "Best Practice PCI DLP Profilee" (typo) vs actual "...Profile" — malformed/mis-published profile fails every scan invoking it; WebUI should have caught the typo. | none | Needs rollback/fix of tenant profile. |
| Tenant Issues | high | both | open | FedRAMP: "DLP Tyoc health check failed" in fed01-mp-prod/fed02-dp-prod; PBMM DLP cache hits = 0. | none | Handed to Wayne Xin to check. |
| Tenant Issues | critical | MP | resolved | Tenant 11557 (AM2, AirBus-related), phased haproxy backend rollout blocked; agreed to go 5→6→8 servers instead of 5→8. | ENG-1067415 | Dedicated haproxy backend (mpdlp31/32) created for tenant. |
| MP Issues | high | MP | open | "Search Alert: More than 1000 found for dppool kvoop segfaults" — count trending up June 21-22. | none | Flagged for investigation. |
| DP Issues | high | DP (+FedRAMP pending) | resolved in prod / pending FedRAMP | ENG-965176: nsdlp crashes traced to `enable-evict-incidents`/`enable-evict-forensics` flags. | ENG-965176, CM-262157, CM-263665, CM-229963 | Flags disabled per-DC (staged rollout after DEL1 soak test); FedRAMP rollout pending. |
| DP Issues | critical | MP | resolved | ENG-969470: nsdlp crashing continuously on VMs not yet on R136 — new DRM-label cache format from upgraded servers is not backward-compatible with R135, poisoning shared cache. Crashes concentrated on tenant 6157 (DRM-label file filters) then also tenant 2736; up to 72 dmp files, 35+ hosts still on R135. | ENG-969470, CM-229393 | Isolated tenant 6157 to 2 already-upgraded servers; pushed full fleet to R136. |
| DP Issues | high | unknown | open | ENG-970341: same incompatible-cache-format bug, in `nsdlpcachesvc` k8s pods. | ENG-970341 | Pending confirmation of pod build version in kubernetes. |
| WD Aborts | critical | MP | pending confirm | ENG-964934: SV5 `nstextsvc` restarting on high watchdog-abort rate — log shows `"NsWatchdog: thread ... is now considered lost due to the 103 times that the thread scan failed to find activity"` (15 occurrences on one host); affected hosts unreachable/very slow (CPU/mem normal). | ENG-964934 | Requested ops reboot affected hosts; resolved after IMF-396 removed 23 bad hosts (unconfirmed in slice). |
| DP Issues | high | unknown | open | ENG-973256: ecr corruption causing crashes in AM2. | ENG-973256 | Pending CS ticket update. |
| DP Issues | critical | MP | resolved | EIMF-396: SV5 R135→R136 rollout blocked by 23 bad/slow-disk servers plus a separate bug where `/proc/sys/kernel/core_pattern` pointed at `/dev/null`, which got deleted (turning core-dump silencing into a device-file deletion) and blocked further deployment. | EIMF-396, CAP-34370 | Moved 23 bad hosts to maintenance/decommission list; PR merged fixing the `/dev/null` deletion; new VMs provisioned via CAP-34370. |
| FP Blockers | high | MP | mitigated | SV5: multiple "blocker" alerts tied to an FP (file-processing) error class. | CM-212704 (precedent ref) | Standard fix: put the affected server into haproxy MAINT mode, restart nsdlp service, then pull back from maintenance (runbook: DLP Server Maintenance in Production Environment). |
| Scan Failures | medium | DP | resolved (partial) | QRO1: adding OCR pods reduced timeouts/`ocrresponsefailures`; plan to extend to other DPs. | CM-224515 | Added OCR pods in QRO1. |
| DP Issues | medium | DP | mitigated | RTM1/RTM2 and `sv5slowmpdlp12/13` running mismatched `dlpsvc.ns`/`textsvc.ns` versions (136 vs 135.1); hosts appeared to not be processing anything. | none | Put mismatched-version hosts in MAINT mode. |
| Other | low | unknown | pending | Ticket ENG-974078 requires enabling feature flag `DLP_ADAPTIVE_DLP_TIMEOUT`, customer wants it live 4/20. | ENG-974078 | Waiting on scheduled enable date. |
| Integration Failures | medium | DP (FedRAMP) | open | fed02 `dlp-fp-config` unreachable — `klbivip-ext` on fed02 not reachable. | SYS-41558 | Ticket filed; OPS reproduces but cause unknown. |
| Integration Failures | high | DP | resolved | SV5 PDD upload/download broken — EDM upload+hashing failing; root cause: duplicate haproxy server names (`upload-1`/`download-1` defined twice) from a buggy Jinja2 deployment template that kept reverting the fix. | CM-230834, CAP-34116, CD-59836 | Renamed duplicate backend entries (upload-2/download-2); CD team fixed the Jinja2 template bug generating the duplicates. |
| Tenant Issues | medium | DP | no action needed | Tenant 11557 profile load failures caused nsdlp crashes; CS ticket updated, no DLP-side action identified. | CS-4286 | none |
| Other | low | unknown | resolved | Rollout of `DLP_MULTI_CI_ENGINES` flag for 4 tenants (23073, 11732, 18212, 21898) via existing provisioner commands. | CM-227445 | Flag enabled via CM. |
| Integration Failures | high | DP | resolved (temporary) | Async DLP enabled on VMs in 6 new K8s datacenters (hnl1, kul1, mct1, scl2, ymq3, yyz3) that should have used pods — caused DNS/network PDV failures and ~21M/day redis errors (ENG-981166); separately nsdlpcachesvc FR4 saw ~1M/day redis errors (ENG-981168). | ENG-981166, ENG-981168 | Disabled Async DLP in the 6 affected datacenters. |
| Other | low | unknown | flagged | OpsGenie P1 page (#372820, re: ENG-971280) routed to ENG-DLP/ENG-DLP-QE though it's actually an EPDLP ticket. | ENG-971280 | Flagged for correct routing; no fix confirmed. |
| Other | medium | n/a | resolved | CM ticket for the evict-flag fix kept bouncing back to "Change Requested" instead of progressing through approval states, delaying a Day-1 crash fix. | none | Manually walked ticket through Technical Review → approval. |
| Tenant Issues | medium | DP | resolved | Tenant 10761 (First National Bank of Omaha): profile "Cyera - Capture All" hit `Overlap limit 128 exceeded`, generating blocker-alert noise; found via `jq` against `dlp_profiles.json` matching the offending `custom/` entity to the profile name. | CM-229963, CM-231874, CS-4312 | Disallowed the "Cyera - Capture All" profile for tenant 10761. |
| WD Aborts | critical | DP | open | US-SJC1: tenant 11863 causing 83% of all watchdog kills (136/164) via dictionary entity `dictionary/d285` — dictionary paths hang the CI thread (unlike regex which fails fast), killing/restarting the whole DLP service; restart rate accelerating 31→46→72/hr. | none | Root cause identified; no fix applied in this slice. |
| WD Aborts | high | MP | mitigated | SJC1 watchdog-abort investigation identified tenant 8517 (Falabella) as top offender (most stalled inspection requests), surpassing AirBus. | none | Enabled EDK chunking for Falabella on 10 email servers (4 SV5, 4 SJC1, 2 DFW3); validated profile_hits unchanged after ~1.5 days before wider rollout. |
| Integration Failures | medium | unknown | resolved | EGS pod issue in KR-ICN1/BR-SAO3 diagnosed (via Claude) as a vault-agent-init vs keystore-init-timeout race condition. | none | Pod self-recovered after 95 min without a restart; no action needed. |
| DP Issues | high | DP | open | IMF-1343: forensic loss due to a Ceph issue in AM2. | IMF-1343 | none stated |
| Scan Failures | medium | DP | open | Tenant 2813 scan-failure percentage steadily increasing over 12 hours in US-PHL1 and US-NYC4. | none | Under investigation, no resolution in slice. |
| DP Issues | medium | DP | open | ENG-985974: DLP process queue-size metric growing unbounded on single hosts from R136 onward (e.g., 688,833,408 on sfo1) — same class of bug as ENG-642902 (previously only seen in non-production stacks). Root cause per lb-stats analysis: counter not decremented in some code path. | ENG-985974, ENG-642902 | Root cause identified; fix status unclear in this slice. |
| Scan Failures | medium | MP | inconclusive | Phoenix team reported "OCR errors" for tenant 9605 (eBay) in SJC1; turned out to be a `CRITICAL: DLP Error Rate Exceeds 20% in POP US-SJC1` alert, not OCR — actual `scan_result=Fail` volume looked low, root cause not confirmed (possibly related to the queue-size metric bug). | SRE-58442 | none confirmed |
| Tenant Issues | high | MP | open | Tenant 8619 (secuvy.com) scan failures in SJC1 — `"Previously loaded config is not valid"` / `"Failed loading dlp profile list"` / `"Failed loading profile configuration"` across a huge list of profiles. | none | none stated |
| Integration Failures | critical | DP | open | EIMF-415: CASP and DLP API continuous synthetic failures in US-SV5. | EIMF-415 | Acked and investigated on a bridge call; no resolution captured in slice. |
| DP Issues | high | MP | resolved | `!!!BLOCKER!!! MP DLP DRM requests failed with status: 0 (CURL timeouts) >= 1000` (peaked 5,334) in US-SJC1 — DRM external service latency spiked 50ms→15,343ms for ~20 min, pop-wide across all hosts/tenants, not capacity/config related. | none | Self-recovered by 18:00 UTC; no action needed, monitor for recurrence. |
| Integration Failures | critical | DP | open | SJC1: random per-host CPU/error-count mismatch (e.g., mpdlp30 low CPU but 11,969 IMS errors; mpdlp10 100% CPU, 1 error) traced to transport between SJC1 DLP hosts and `apiconnectorsvclb`/`imsapisvclbaasvip-int` truncating large `/forensic` multipart uploads mid-stream — IMS returns 400/`Missing event_file`, LBaaS drops connection with no headers; 16,974 incidents lost forensic payloads (fell back to GEF, metadata only). | none | Escalated to IMS/LBaaS ops; recommended checking uwsgi access logs and haproxy client/server timeouts and body-size limits on the `/forensic` path. |
| DP Issues | medium | DP | resolved | New host `dlppool1-10.maa2.nskope.net` crashed on DLP service — same evict-flag class of bug as ENG-965176, flags not disabled on new node. | CM-235593 | Change reviewed/approved and completed to disable the 2 evict flags. |
| Tenant Issues | high | DP | mitigated | EIMF-416: tenant 1633 (Slack Enterprise namespace) hit `Overlap limit 128 exceeded [entity:custom/c0287ee...]` and multiple scan failures ("High income" profile); large 28MB PDF also failed text extraction (`rc=19`) in same window. | EIMF-416, CM-235509 | Phoenix team mitigated by decreasing `maxinflight` count (PR #14682); DLP-side root cause (load vs. profile) investigation ongoing; alert threshold (`scan failures by host and tenant[10m]==0`) flagged as needing revision. |
| Scan Failures | low | DP | resolved | FR4 email DLP timeout alert self-cleared — combination of very low traffic and a handful of failed scans. | none | No action required. |
| MP Issues | critical | MP | documented (runbook) | IMF-1345 (SJC1): recurring curl-thread saturation on AMD mpdlp machines behind `mpdlphaproxy02.sjc1`. | IMF-1345 | Documented recovery: copy `mpdlpsvc.haproxy.cfg.IMF1345` (or `.Aggressive` variant) to `/etc/haproxy/mpdlpsvc.haproxy.cfg` to shift iaas machines off the AMD pool and cut their maxconn, `sudo service haproxy reload`, monitor recovery, then revert config and reload again; capture change via IMF channel + CM ticket (created after mitigation, not before). |
| DP Issues | high | DP | resolved (auto-mitigated) | JP-NRT3: nsproxy `RESP_TIMEOUT`/`DROP` alert firing for 3 hours, traced to a Ceph issue (channel `eimf-426-fr4-ceph-issue`); auto-mitigated, full RCA pending with cloud team. | none | Auto-mitigated; cloud team investigating RCA. |
| Integration Failures | high | DP | open | "Lots of IMS post failures on multiple DPs" — being checked jointly with IMS team. | none | Investigation in progress, no resolution in slice. |
| Integration Failures | high | DP | mitigated | EIMF-430: BOM3 k8s out of schedulable resources — `dlp-fp-upload` pods stuck Pending (`0/15 nodes available: Too many pods, Insufficient cpu`), affecting TYOC/fingerprint 2.0 services. Root cause: scheduler blocked on request-budget exhaustion, not actual CPU load. | OPS-268117, EIMF-430 | OPS scaled OCR pods down from 2 to 1 to free CPU budget; PESRE engaged for long-term fix; did not qualify for full IMF. |
| Tenant Issues | medium | DP | resolved | Tenant 11924: `"Profile Protected Health Information (PHI) Detection not found"` error in `ns_dlp_cfg_profile.cpp`. | CM-238433 | Profile disallowed for tenant 11924. |
| Scan Failures | medium | DP | open | Tenant 1977: 390KB PNG scan took 278s total (`fextract_time=279919ms`) — text-extraction slowness. | none | Investigated via Sumo query; no fix captured in slice. |
| Tenant Issues | low | DP | open | Tenant 17109 profile load failures reported. | none | Minimal detail; no resolution captured. |
| MP Issues | high | MP | mitigated | EIMF-431: `mpdlp06.sv5` newly added to the LB without running the validation script or DLP-team approval — missing `telegraf` (so no alerts fired despite failures), incomplete EDM sync (`total=102860, handled=42056, expected=46287`), missing packages, unclear version `136.0.4.3147`; caused 104,760 `"Failed to load tenant"` log lines for multiple tenants. | EIMF-431, CAP-15926, CM-135494 | Host put into maintenance mode; CM follow-up tracked for proper onboarding. |

**Recurring alerts (slice 2):** EGS ZMQ send-failure >5% (3, usually benign LB-eviction retry noise); Profile Load Failures per tenant >10 over 3x15min (3+); haproxy backends down (2, ignore-list of known low-VM DPs applies); GEF forensics-loss 0-5% (2); DLP unable to send Incident to IMS timeout >100 (1, no runbook at time); Icinga FLAPPINGSTART on `dlp-resulthandler-port-80` (1); textsvc multiple versions in pop (1).

**Sumo/query artifacts (slice 2):** `_code=WmWPNLBiB5av6YCBD0O5YxawHTnUMPwgmCeDgCrS` counting SV5 TE-timeout hosts; `_index="dlp_us_sv5_debug" "Text extraction did not succeed" | count_distinct(_sourceHost) as unique_count`; `_code=HJczwNEE9j81GXLkZuV2KpXM1SSIGJbUDjU2nrfu` async-DLP redis error volume; `_code=qrvDZhy0W85x3d49P2EID9LZtCVGmW5OGK0HLaX4` slow-scan "All Scan Info" line pull. Note: `_code=` links are ephemeral saved-search permalinks, not reusable templates.

---

## Slice 3 (id 1009-1512, ~2026-04 to 2026-05-22) — 12 entries

| Category | Severity | Plane | Status | Summary | Jira | Action Taken |
|---|---|---|---|---|---|---|
| FP Blockers | High | Unknown | Open | EDM false-positive impacting Morgan Stanley. | ENG-936898 | Direct customer reply. |
| WD Aborts | High | DP | Resolved | `nstextsvc` crashing on restart, root-caused to "Failed setting up tmpfs" (3rd recurrence). | ENG-1033874 (dupe of ENG-861143, ENG-548870) | RCA filed; standard check = `/var/crash` + grep for FATAL tmpfs message. |
| DP Issues | High | DP | Open | DRM CURL-timeout alerts across EU DCs + SJC1 post-R137: DRM and slow IMS/forensic posts share one CurlSvcThrClient taskpipe. | ENG-948761 | Diagnosis: `log level CURL 1`, grep `queue_delay=`; long-term fix (split taskpipes) not implemented. |
| DP Issues | Critical | DP | Resolved | `dlppool1-1.bog1` sustained BLOCKER RESP_DROP (11.95%) — host-local malformed/aborted responses, not capacity. | CM-249178 | Host cordoned from HAProxy; dlpsvc restarted. |
| MP Issues | High | MP | Resolved (mitigated) | RUH1(+SIN2/BOM3/MEL2) LBaaS FLAPPINGSTART on `dlp-resulthandler-port-80` post R138 async-DLP deploy — softirq saturation + orphan Helm test-connection pods sharing the Service selector. | CD-61640, ENG-1028497 | Deleted orphan pods; added `helm.sh/hook-delete-policy` to 4 charts; gated the helm-test pipeline step. |
| Scan Failures | Medium | DP | Resolved/explained | Tenant 13446 (+22425, 19494) timeouts vs NSProxy `client_timeout=10s`: CI legitimately takes ~25s mean on ~6.7MB text. | none | Documented as expected behavior, not a fault. |
| Tenant Issues | Critical | DP/MP | Resolved | Tenant 20955 profile updates cascaded Profile Load Failures fleet-wide — timing issue in new config-loading framework. | ENG-1009495 | Forced global refresh via `curl -vvv http://dlpcfgpushervip:8887/config/refresh/20955`; isolated-host cases fixed via nsdlp restart script; runbook published. |
| Integration Failures | Critical | MP | Partial | BLOCKER EGS ZMQ alert-send failures (SIN2, tenant 27036): IMS 202 misclassified as errors; EventForwarder silently dropped 212 ZMQ messages failing schema validation. | none | EG disabled ASAP; bug filing requested for silent-loss gap. |
| Integration Failures | High | MP | Resolved | EG BLOCKER ZMQ + IMS post failures (IL-TLV1) root-caused to LB (PLBI) evicting idle (>60s) connections. | ENG-1007862 | Fix = PR #597: refresh workers idle >50s before LB eviction; merged to develop, backport needed. |
| Integration Failures | High | DP | Resolved | Tenant 3256 forensics lost to GEF since 6am (SV5/DEL1): IMS HTTP 424 "Forensic profile read error" — customer misconfig. | ENG-1013227, CS-4441 | ENG ticket to IMS team; CS ticket to notify customer. |
| Integration Failures | High | MP | Resolved (rolled back) | After a week-long EG-for-ASP rollout across 12 DCs, cascading forensics-loss incidents forced a full rollback everywhere. | none | "EG for ASP has been disabled in all datacenters it was previously enabled in." |
| WD Aborts | High | MP | Mitigated | SJC1 WD-abort investigation: tenant 8517 (Falabella) top offender. | none | EDK chunking enabled on 10 email servers; validated profile_hits unchanged before wider rollout. |

**Recurring alerts (slice 3):** EGS ZMQ send-failure >5% (usually benign, PR #597 fix); Profile Load Failures per tenant >10 over 3x15min; HAProxy backends down warning (ignore-list applies); GEF forensics-loss 0-5%; textsvc multiple versions in pop.

**Sumo/query artifacts (slice 3):** No literal Sumo query syntax found (grep-verified) — only informal mentions of running a Sumo query, and a note that DLP service logs were missing from Sumo entirely for `mpdlp12` in SIN2 (MON-24244 filed).

---

## Slice 4 (id 1513-2015, ~2026-05 to 2026-06-30) — 51 entries

| Category | Severity | Plane | Status | Summary | Jira | Action Taken |
|---|---|---|---|---|---|---|
| DP Issues | medium | DP | resolved | DNS resolution failure on dlphaproxy01.bog1 caused dlppool1-* connection timeouts (dlphaproxy02 unaffected). | OPS-273361 | OPS ticket raised for DNS fix. |
| DP Issues | critical | DP | resolved | BOG1 KVM host hit high CPU; OPS "MFO'ed" the site, ~1.6M scans dropped before mitigation; a same-day repeat from a dlppool1-1 duplicate-IP conflict ran 6 more hours. | IMF-1366 (superseded by EIMF-497), CM-254204 | dlppool1-1 put into haproxy maintenance mode; BOG1 re-enabled; leadership mandated IMF/EIMF (not bare OPS tickets) for threshold-crossing prod issues going forward. |
| Integration Failures | medium | DP | recurring | DP hosts dlppool1-5.hkg1, dlppool1-12.bcn1, and dlppool1-7.osa2 (osa2 re-flagged weeks later) ran old build 137.0.0.3736 with EG enabled by default and known forensics-loss bugs. | OPS-277665 (latest) | Disable EG + upgrade to 137.1.1.3249 each time found; no permanent prevention yet. |
| Scan Failures | medium | DP | unresolved | Scan timeouts for tenant 15152 in SCL2 (too many requests for 2 pods) and tenant 13446 in NRT3 (daily 1-2pm PT traffic surge). | none | None — root cause not pinned down. |
| Scan Failures | medium | DP | unresolved | ~2,800 DLP scan requests failed in BUE1 over 3 hours. | none | Escalated to Kuldeep Shankar Melligeri; no resolution captured. |
| WD Aborts | medium | DP | tracked | Watchdog aborts observed in DC MAN1. | ENG-1042596 | Ticket filed. |
| Integration Failures | low | DP | resolved (explained) | Recurring "vault sealed or unhealthy" CRITICAL alerts across several EG POPs (SAO3, LIM1, DFW1, YYC1, YYZ3) traced to normal Vault seal/key-refresh cycles, not an EG bug. | none | None needed — readiness probe fails closed so bad traffic isn't served; proposed dlpcertservice local-key fallback but rejected on security grounds. |
| Scan Failures | high | DP | resolved | SCL2 sustained high CPU/HOL-blocking initially blamed on a suspected `.*` wildcard regex in tenant 8517 (Falabella)'s BFCL_R04_R05 profile; no true wildcard found — root cause was Falabella doubling traffic (large 64-128MB spreadsheet uploads). Project Frigate's EDK chunking meant stuck processes rode out an 8-min timeout instead of being cleared by watchdog. Recurred as capacity-blocked EIMF-501 until new pods were added. | EIMF-501, OPS-276566 | Requested capacity via #dlp-capacity; new pods added; EIMF-501 mitigated. |
| DP Issues | low | DP | explained | No minidump emails from DLP servers: textsvc crashes on tmpfs-creation failures during VM resize (auto-recovers), and minidump isn't enabled for textsvc (produces full core dumps instead). | none (tickets said pending) | None yet beyond explanation. |
| DP Issues | high | DP | resolved | Forensic-loss alerts traced to a prior-day haproxy config change that reduced max-connections while pulling IaaS mpdlp servers from the pool. | none | Re-added 8 IaaS mpdlp servers, then restored the prior max-connections config. |
| DP Issues | high | DP | opened | Repeat of a prior EIMF: heavy forensics loss in SJC1, some incidents routed via GEF. | EIMF-504 | EIMF filed and monitored. |
| DP Issues | medium | DP | unresolved | nsdlp crash dump on mpdlp03 — stack trace shows crash in `ns_dlp_cfg_tenant_inotifier_cb` inside a pthread_rwlock_unlock during the FileNotify kernel task. | none | None beyond sharing the dump. |
| Scan Failures | medium | DP | resolved | Second firing of DRM alerts (1,142 timeouts first time) plus high resp_timeouts in the same DC, root-caused to elevated scan_ctx across multiple tenants together. | none | Monitored; trended down on its own. |
| Tenant Issues | medium | DP | open | Tenant 17175 hitting persistent profile-load errors across multiple datacenters. | none | Mitigation question posed, left unanswered in-thread. |
| DP Issues | low | DP | resolved | A FRA1 deployment silently reverted oop_method to legacy 16h earlier; before/after TE-error comparison showed the bump was mostly higher traffic, not the regression. | none | None required. |
| DP Issues | high | DP | resolved | RESP_DROP/traffic spike in IE-DUB1 traced to dlppool1-4.dub1.nskope.net never having been added to the haproxy pool. Losses: 2,984 total incident loss, 638 in a separately-tracked bucket, 62 forensics loss, plus 532 GEF incident loss from an unrelated DNS issue. | none (tracked as "an EIMF") | Host validated then added to the load balancer via a CM ticket. |
| Integration Failures | high | DP | open | Prolonged LBAAS connectivity problems caused dlp-event-generator incident-posting failures at both SJC1 and ZUR2. | OPS-275256 (SJC1), OPS-275264 (ZUR2), CM-258569 | OPS tickets filed per datacenter. |
| FP Blockers | high | DP | unresolved | DLP scan drop in YYZ1: `Overlap limit 128 exceeded [entity:computing/security/passwords/secure]` and `CI based scanning failed` for profile PDSB-Suspected Compromised Events. | none | Flagged to Shikha Kumari/Nirendra Awasthi; no fix captured. |
| Tenant Issues | medium | DP | unresolved | DLP scan timeouts for tenant 7901 (Baincapital) lasting over an hour. | none | None captured. |
| Other | low | unknown | unresolved | 11 OpsGenie "DLP TYOC healthcheck" CRITICAL alerts fired across FedRAMP/PBMM stacks (pbmm01/02, dfw5, sjc3, iad5, fed01/02) with nothing matching the on-call rulebook. | none | Escalated to US team — India lacks Prism access to compliance stacks. |
| Tenant Issues | critical | DP | resolved | Tenant 24424 (James Madison University) threw "Failed loading profile configuration, ScanCtx is invalid" across many DFW3 servers — 245,981 failures over ~2 hours. | EIMF-523 | Refreshed the tenant's profiles fleet-wide to mitigate. |
| DP Issues | medium | DP | escalated | Only 1 of 2 dlp-core pods schedulable on RTM1/RTM2 due to insufficient CPU and control-plane node taints. | none | OPS ticket filed; posted to #dlp-capacity for more node capacity. |
| Integration Failures | high | DP | resolved | EG blocker alerts at SIN2 (S3 uploads failing) plus DRM request failures on SJC2, concurrent with the SIN2 ceph incident. | none | Escalated; updated the DLP Event Generator Runbook (Confluence). |
| Other | low | unknown | resolved (false alarm) | Apparent nstextsvc "crash swarm" across pods was actually euc1/euc2/apse2 — public-cloud POPs not yet live — generating noise from missing logs. | none | Confirmed not-live with infra (Fernando Eickhoff); safe to ignore alerts for those POPs. |
| DP Issues | critical | DP | resolved | SYD1 haproxy host became overloaded, degrading traffic. | none (tracked as "an eimf") | Failed over to haproxy02; OPS still needed to migrate the host to a different vmhost. |
| DP Issues | low | DP | accepted | Recurring daily proxy-timeout alert on fr4 email servers from host overload. | none | None — accepted as a known daily pattern. |
| Integration Failures | medium | DP | escalated | 11.5% packet loss on the SCL1→SJC1 (IMS endpoint) path caused ~25 forensics-post timeouts. | OPS-275920 | OPS ticket filed for the packet loss; cross-posted to #pe_public. |
| Integration Failures | critical | DP | resolved | SIN2 Ceph had a nearfull OSD (~85%) whose PG rebalance saturated the write path shared with the dlp-sg-sin2 S3 bucket, stalling EG forensic PUTs (25% failure rate) for 8+ hours across ~12 tenants (tenant 25904 ~90% of failures). | CLOUD-10549 (Blocker), OPS-275976 | Escalated to Cloud/Mirantis to shed PGs off the OSD; confirmed no EG code bug, no code change made. |
| Integration Failures | high | DP | resolved | Scheduled Cogent network-maintenance across multiple regions disrupted EG forensics uploads. | none | EIMF opened; auto-resolved after the maintenance window. |
| DP Issues | low | DP | resolved | P1 alert for only one running nsdlp process in DE-FRA1 after a service restart. | none | Monitored ~30 min; all 3 processes came back, no crash evidence in /var/crash. |
| Other | medium | DP | open | A nightly cron job restarts the dlppool process on every DP host to counter unbounded memory growth — meaning scan requests are intentionally dropped fleet-wide every night. | none | None — flagged as needing a real fix, not just the cron workaround. |
| Integration Failures | medium | DP | open | Repeated short spikes of DRM-send errors in SJC2, partially correlated with CLS timeouts. | none | DRM team notified; suspected same DLP-overload-slows-curl pattern as the earlier SJC1 issue. |
| Tenant Issues | medium | DP | resolved | A bug on a single MRS1 pod caused Airbus DLP profile-load failures. | ENG-681462 | Restarted the dlp service on the affected pod. |
| Tenant Issues | high | DP | unresolved | 131K Morgan Stanley (tenant 15862) scans failed on dlppool1-8.maa1 with "dlp cfg base not found" — the 2am profile sync landed on disk but the running process never reloaded it. | none | Logs investigated; no fix stated in-thread. |
| Tenant Issues | low | MP | resolved | Phoenix team requested DLP_MAX_CONCURRENT_SCANS 5→8 for Airbus on AM2; an older ticket recommended staying at 5, but capacity on mpdlp31/32 showed headroom. | ENG-973732 (older, contra), ENG-1067415 | Approved a phased ramp (5→6, then 6→8) instead of jumping straight to 8. |
| Scan Failures | medium | DP | inconclusive | Intermittent "Failed to extract detailed response event" (buflen=0) on several ymq1 dlppool hosts; TE never logged the request (suggesting an ASP drop between DLP and TE) — but may be normal long-timeout behavior for gzip/compressed files. | none | Root cause left unresolved between the two competing theories. |
| DP Issues | high | DP | resolved | The same phx-pod autoscaling behavior triggered a second EIMF. | ENG-1068801 | Ticket filed to address the autoscaling behavior. |
| DP Issues | high | DP | escalated | CO-BOG1's dlphaproxy01 stopped responding to any command (including supervisorctl) though still reachable by ping; one of 5 dlppool servers also down. | OPS-276548 | OPS ticket filed, citing a prior related network issue. |
| Other | low | DP | resolved | The "DP DLP servers down" Sumo alert paged many engineers who weren't on-call; it was a scheduled-maintenance window mis-flagged by the query, and the alert config hadn't been touched in 14+ days. | none | Venkateshwara Reddy applied a temporary fix to restore normal alerting. |
| Tenant Issues | medium | DP | resolved | Tenant 14714 (UK-LON2) profile-load failures (~1.5% of that tenant's scans) traced to a mis-spelled DLP profile name in their policy ("Profilee" vs "Profile") — a WebUI validation gap. | none | Root-caused; fix routed to tenant-config team. |
| DP Issues | high | DP | escalated | fed01/fed02 lookup services can't reach fed02 dlp-fp-config because klbivip-ext on fed02 is unreachable. | SYS-41558 (Blocker) | Ticket filed and marked Blocker as production-impacting. |
| Integration Failures | medium | DP | open | WAW1 dlp-dlp-async pods (result-handler, scan-handler) evicted from a node with no ephemeral-storage requests/limits configured; a separate scan-handler pod earlier crashed on a mount-permission error. | none | Unresolved — needs delete permission to clean up evicted pods and ephemeral-storage limits to be added. |
| WD Aborts | medium | DP | resolved | New incident/forensics-eviction flag trialed in DEL1 to check for process crashes this area caused before; some watchdog-abort restarts in the prior 24h were judged acceptable. | CM-262157 (trial), CM-263665 (fleet rollout) | Enabled DEL1 first, then MAN1/STO1/BKK1/DXB1/PER1, remaining DCs staged for following week. |
| FP Blockers | high | MP | recurring | Per-tenant HS/CH (Hyperscan/Custom-Hyperscan) regex-compilation errors have now hit 4-5 tenants, each needing a manual on-call fix. | CM-263723 (latest) | Disabled HS/CH regex compilation in commercial MPs; Wai Yim tasked same-day to write a runbook so any on-call engineer can self-serve the fix. |
| DP Issues | medium | MP | resolved | An alert first thought to be another HS/CH tenant issue turned out to be a DNS regression from change CM-263027. | ENG-1074689, CM-263027 | Rolled back CM-263027; DNS resolved; PESRE team to own RCA. |
| Tenant Issues | high | DP | open | Tenant 24727 hit >10 profile-load failures across three 15-min windows (37 at time of posting). | none | "Will be checking the details" — no resolution captured. |
| MP Issues | high | MP | resolved short-term | All SJC1 MP-DLP servers hit 100% CPU again — different from the earlier iaasmpdlp stall that caused a prior IMF. 30-day trend: episodic ~3hr afternoon surges roughly every 10 days across ~21 of 72 hosts, no single noisy-neighbor tenant. | none | Manual mitigation already in place (iaasmpdlp hosts moved to an introspection pool); recommended correlating surge windows with per-tenant traffic. |
| Other | low | unknown | open | OPS-275762 has three associated CAP (capacity) tickets needing follow-up confirmation. | OPS-275762 | None yet — question raised, unanswered. |
| Other | low | DP | resolved | 22 "AFTER-maintenance" alert firings across 8 POPs tied to the planned EG-enablement rollout (CM-263006). | CM-263006 (context) | Confirmed as expected post-change noise, not a new problem. |
| WD Aborts | low | DP | informational | Project Frigate's EDK chunking changed nsdlp behavior: processes that used to be cleared quickly by watchdog now ride out an ~8-min timeout before clearing. | none | None — documented as an intentional behavior change (feeds into the SCL2/Falabella confusion above). |
| WD Aborts | medium | DP | unresolved | Two watchdog aborts recorded in nstextsvc on dlppool1-1.fra1 ("aborting process due to thread(s) that are not responding"); a separate WD check-in miss on the same host was confirmed NOT the cause of an abort. | none | None captured beyond sharing the logs. |

**Recurring alerts (slice 4):** Saved Search "CRITICAL: DP DLP servers down (haproxy logs)" — canonical query captured verbatim (see SKILL.md catalog), fired at least twice with different root causes (DNS, mis-flagged maintenance); "CRITICAL: Event Generator Errors Report"; "DLP Profile Load Failures Per Tenant > N" (5 distinct tenants); Hawkeye FedRAMP/PBMM synthetic checks (2 batches, 11+ alerts); "DP DLP Scans Timeout By (Pop,Tenant) Over Incoming > 5%" (5+); "Blocker: DP DLP Failed to read TE response" (tmpfs-full check); "More than 1000 found for dppool kvoop segfaults" (daily digest).

**Sumo/query artifacts (slice 4):** Canonical haproxy-down saved search — full pipe is in the Sumo Query Prefix Catalog table in `../SKILL.md`; `(_sourceCategory=*dp_nsdlp_svr or _sourceCategory=*dlp-core) "Failed to extract detailed response event"`; `_index="dlp_uk_lon2_debug"` tenant grep; OpsGenie Hawkeye query for FedRAMP/PBMM; several ephemeral `_code=` permalinks (Incident Loss, Forensics Loss counts during IE-DUB1 EIMF).

---

## Slice 5 (id 2016-2399, 2026-06-30 to 2026-07-05, plus 10 live `/nsoncall` triage sessions 2026-07-04 to 2026-07-08) — 27 entries

| Category | Severity | Plane | Status | Summary | Jira | Action Taken |
|---|---|---|---|---|---|---|
| Integration Failures | critical | both | resolved (rolled back) | EG-for-IMS queue backed up in BOS1/DEN1 (2,150 forensics lost across tenants 22751/20268/23310) and DFW3 (IMS timeout retries, 8,233) — the S3 object-purger deleted source objects before the backlogged EG queue could drain them, permanently losing forensics; DFW3 traced to a separate bug. Escalated as combined IMF/EIMF. | ENG-1083313, IMF-1383, EIMF-566, CM-263006, CM-257164 | Disabled `enable-event-generator-for-ims` (+ override flag) fleet-wide across every DP/MP datacenter over several days; standing policy to keep EG disabled in prod pending fixes/resourcing. |
| Integration Failures | high | both | recurring/mitigated | Post-"disable everywhere" stragglers: SJC2 kept sending to EG for tenant 2132 (override flag left true on some hosts); DFW4 Redis-queue buildup (27% memory) + pod memory to 94%; EG pods stuck Pending in DFW3/BOS1 from insufficient k8s node CPU, causing `failed to decrypt symmetric key` forensic drops. | OPS-278392, CM-265666 | Re-ran disable script per host until confirmed false in `nsdlpsvc.cfg`; identified tenant 2132 (Netskope Corp) + tenant 17093 (Deloitte) as intentional SMTP-proxy Async-DLP test traffic; DFW4 self-recovered. |
| DP Issues | high | DP | resolved (mitigated) | `dlp-core` pods crash-looping in MRS1 then PAR2/PAR3, all tenant 18821 — root-caused to `FileCtxLegacy::dlp_scan_run_ocr()` constructing `std::string(nullptr)` from an unset `sub_file_path` when OCR runs on an image subfile whose on-disk path was never populated (`FileCtxLegacy.cpp:612`), throwing `std::logic_error`. | EIMF-563, ENG-1082440, CM-265138, CM-265497 | Disabled OCR for tenant 18821; null-guard fix proposed, not confirmed merged in slice. |
| Scan Failures | medium | MP | recurring | "Overlap limit 128 exceeded" resurfaced for tenant 14940 (Japanese-prefecture entity, EDK-affected); separately a widening trend of tenants deploying blanket `.*` catch-all profiles to snag AI-agent/chat traffic (tenant 12618: Claude/ChatGPT/DeepSeek/Gemini/Grok/Perplexity profiles) — fail often but still catch some. | CM-265306 | Tenant 14940 alert self-resolved, CS ticket discussed; one `.*` profile disabled via CM-265306; no team decision yet on the broader AI-profile trend. |
| WD Aborts | medium | DP | resolved (root-caused via /nsoncall) | LON1: 3 watchdog aborts split between tenant 17800 (Viasat, new offender) and tenant 4673 (Airbus, repeat offender). Tenant 17800's `custom.xml` entity has an unbounded greedy `.*` between a common anchor and a 12-way alternation, causing catastrophic backtracking on a 100MB CSV; `uot-ci` thread hung ~8.5min past the watchdog limit. | none | Recommended EDK-chunking for tenant 17800; verify it's still on for tenant 4673; file CS/CX ticket to bound the `.*` to `.{0,200}`; no IMF/EIMF (steady 1-2 aborts/30min). |
| Tenant Issues | medium | both | open | Tenant config `enable-drm-svc` jsonparse error across multiple tenants — value stored as `1` instead of boolean `true`. | ENG-1084964 | Flagged as matching a known recurring boolean-serialization tenant-config bug class; no fix confirmed in slice. |
| MP Issues | medium | MP | resolved | `dlppool1-3.tpe1` `nsdlp` OOM-killed by kernel; all TPE1 nsdlp servers restarted same window. | none | supervisord auto-respawned `nsdlp_0`; haproxy confirmed all 3 TPE1 backends back UP. |
| Integration Failures | medium | DP | open (no runbook) | MY-KUL1 Async DLP Redis-pool alerts (`pool regular has no connected Redis clients`, sustained consecutive Redis failures on Scan Handler) — alert payload doesn't distinguish the two Async-DLP Redis clusters (cache vs. queue). | none | On-call unable to determine target Redis service from alert alone; unresolved. |
| DP Issues | medium | DP | open | IE-DUB1 dropped DLP scans traced to one haproxy backend host pinned at 100% CPU, out of LB rotation. | ID-978 | PE team engaged to CM the host back into rotation; not confirmed complete in slice. |
| Scan Failures | low | MP | resolved (auto) | MX-QR01 (QRO1) profile-load failures isolated to a single pod (`dlp-core-3`); self-resolved by time of review. | none | No action required. |
| DP Issues | medium | DP | open | QRO1 Text Extraction service ran out of memory. | CAP-39616 | CAP ticket filed; ops contact pinged. |
| Tenant Issues | critical | both | recurring | Tenant 18823 (Banco Santander) driving repeated WD restarts, resolved once via EDK-chunking sign-off — then recurred 2026-07-06 in BR-SAO1: `uot-ci` threads hung on the tenant's ~40-profile stack (many overlapping "exclude" variants), CI runtime exceeding `client_timeout` (6.10% scan-timeout rate, no aborts, no forensic-post attempts — detection-latency, not data loss; step-jump 15x at 12:00 UTC). | CM-265290 (first pass); none filed yet (second pass) | First pass: EDK-chunking enabled after 2-approver CM sign-off. Second pass: EDK-chunking re-recommended (CM-gated, Emanoel/Niranjan/Abhiram signoff pending); CS/CX ticket suggested to consolidate Santander's redundant profile set. |
| Other | medium | unknown | open/process | EIMF-569 mass-paging: OpsGenie escalation path paged the entire on-call team (not just primary) for a P1 with only 19 forensic-loss occurrences, below the documented EIMF threshold; initial bridge call had no one join despite multiple pages. | EIMF-569 | Leadership directed the IMF commander to stop blasting the whole team and shut down further alerts; no OpsGenie config change confirmed. |
| Other | low | DP | resolved (benign) | GVA1 "100% Unknown_Fmt" BLOCKER alerts, all tenant 5566, on two separate hosts/days: dlppool1-1 traffic is ~66-100% legitimate `application/octet-stream`; dlppool1-7 is Adobe After Effects `.aecache` disk-cache files. Both correctly classified by KeyView (not a service fault), evenly hash-distributed across all 7 GVA1 hosts, `scan_result=Success` throughout. | none | No DLP action either time; recommended alert-tuning ticket (exclude octet-stream/`.aecache` from the Unknown_Fmt bucket, or gate on absolute count+ratio); proposed new playbook row for this alert class (added to SKILL.md). |
| Scan Failures | low | unknown | unclear | MUC1 DLP timeout errors persisting ~6 hours (only 1 forensic loss in 3 hours); documented tmpfs-full runbook step checked but found no actual disk-space issue (`/var/crash` 1%, `/dev/shm` 1%). | none | No resolution captured; runbook check ruled out as cause. |
| Integration Failures | low | unknown | recurring (query tuned) | "ZMQ ROUTER Send Error: [11] Resource temporarily unavailable" alert kept false-positiving, matching the literal string in unrelated non-DLP (`TASK`-module) log lines rather than genuine dppool ZMQ errors. | CM-265292 (one-off restart), cf. ENG-466009, SRE-52270 | Re-scoped the owning Sumo saved-search to require `"TASK"` AND `"ns_zmq.c"` alongside the error string; restricting to ERROR level discussed, not yet implemented. |
| Other | low | unknown | resolved | `mpdlp25.fra2` `nstextsvc` process restarting; OPS ticket marked fixed. | none | Requested validation before returning host to rotation. |
| Tenant Issues | medium | MP | open | SJC2: >40% proxy-timeout alert for ~5-10min from a traffic spike on tenant 15152 (avg 20K→80K), attributed to long-running inspections. | none | No fix captured; investigation cut short by a separate SAO4 blocker alert. |
| Other | low | unknown | open | Recurring "FAIL: vault keystore unreachable" / "EGS vault unhealthy" alerts continuing in DEL2 (after EG already disabled there) and in BOM2/CCU1/MAA2/HYD1 — unclear if stale/benign post-EG-disable or a genuine Vault issue. | none | Open questions posed to team at end of slice; unresolved. |
| Other | low | unknown | unclear | Old bind-error nsdlp-startup ticket (ENG-790611) — on-call asked if anyone's seen it recently before closing as stale. | ENG-790611 | No confirmed recurrence reported; closure pending. |
| DP Issues | critical (alerted) / benign | DP | resolved-benign | DE-FRA1: 3-backend `dlp_be`/`dlp_rest_be` pool flapped DOWN 2-10s each (L4 health-check) across all 3 hosts over 25 min — rolling-deploy signature, fully healthy 3/3 by 08:11:04. No crash, no scan loss. | none | Acked as benign; flagged saved-search parse-regex brittleness (fra1's `dlppool1-N-0` naming breaks the catalog's literal `-0..` anchor) to alert owner Venkateshwar Reddy; suggested throttling rolling-deploy concurrency to 1-at-a-time. |
| Scan Failures | warning | DP | active, decaying | DUB1: tenant 16099's CI timeouts (`ns_dlp_ci.cpp:01183`) saturated the 4-host DLP pool, cascading to proxy-side `ASP client for DLP scan timed out` for all tenants. Peaked 05:30 (4,966/15min), decaying by 05:45. Dictionary/heavy-content class, not overlap-limit. | none | Recommended monitoring (decaying); CS ticket for tenant 16099's profiles/entities; EDK-chunking flag; isolate to dedicated pool if recurs; no IMF/EIMF (not climbing). |
| Tenant Issues | critical | DP (k8s dlp-core pods) | active, climbing, escalate | FR-MRS1: broken EDK entity `computing/security/secrets/postman_keys` in tenant 18125's (Malakoff Humanis) "MH Custom DLP Profile - Secret data" profile → `EdkAddTargetEntity ret=212` → entire profile list marked FAILED → 91.89% scan failure across all 4 MRS1 pods (5-POP blast radius: par3/mrs1/par1/jnb1/cpt1, single-tenant only). Config-refresh VIP resolved to the wrong POP (San Jose), and even a working refresh would re-pull the same broken config. | none filed yet | Root-caused to the exact entity via statistical co-occurrence across raw failing-config samples; escalated to CE to fix/remove the entity; IMF/EIMF recommended; EDK-load non-atomicity flagged as an ENG follow-up. |
| Other | info (no alert) | DP | closed, answered | LON1 ad-hoc question: which of 3 named profiles does tenant 4673 (Airbus) use. Answer: BSN-1024 and CACP-2027 both in active use (457/24h, 25,408/7d each); ENT-3033 unused (0 hits). | n/a | Answered via one `_index` conditional-aggregation query; session self-flagged 5 chargeable queries (~$2.10) as overrunning the "one scoped query" cost rule. |
| WD Aborts | warning | MP | active | MPDLP12-DFW3: tenant 29505's CI workload pinning `uot-ci` threads fleet-wide (check-in warnings on all 26 DFW3 MP hosts); mpdlp12 alone crossed into 5 actual process-killing aborts in ~1h, all tenant 29505 (distinct PIDs per abort). No overlap-limit errors — dictionary/heavy-content class, not broad-regex. | none filed yet | Attributed to tenant 29505; recommended EDK-chunking flag, then a dedicated haproxy pool if it recurs; IMF/EIMF recommended given fleet-wide + recurring actual aborts. |
| Integration Failures | blocker | MP | active, climbing, escalate | SG-SIN2: IMS forensic-POST `resp_400` (transport truncation, not validation error) spiking pop-wide across 11+ SIN2 MP hosts, 10 tenants affected (23042 66%, 25904 27%). Rate climbed ~7x in an hour then 3.4x more in 30 min (up to ~1,025/min). `final=true` still zero (LBaaS retries absorbing) but imminent if unmitigated. Root locus: `imsapisvclbaasvip.sin2-mp-prod...:8000`. | none filed yet | Escalated to SIN2 IMS/LBaaS + Cloud/storage; IMF/EIMF recommended now per the accelerating-trend rule; no DLP-side fix needed (retries correct); monitor for `final=true`. |
| Scan Failures | blocker | MP | active, climbing, escalate | SIN2: tenant 25904 (Flipkart) bulk-uploading oversized files (`.csv.gz`/`.xlsx`) via CASB API exceeding the 180s upload ceiling, retrying the same files in a loop — 3,894 timeouts/1h, 26x the next tenant, evenly spread across all 23 hosts (draining any host would worsen it). Successful scans cap at 33.5MB. | none filed yet | Recommended filing IMF/EIMF immediately; CS/CX contact to Flipkart to shrink/throttle uploads (<30MB target); optionally raise tenant `client_timeout`; explicitly do NOT drain any host. |

**Recurring alerts (slice 5):** EGS/EG family alerts (S3 download failure, Forensics Lost, vault connectivity/keystore unreachable, EGS vault unhealthy, IMS post-attempt failures, Async max-scan-attempts-reached, Redis pool disconnected/consecutive-failures, pod Pending/CrashLoopBackOff) — 15+ distinct firings across the multi-day EG-for-IMS saga; "Overlap limit 128 exceeded" (2); "ZMQ ROUTER Send Error" (5+, mostly false positive); FATAL/crash-dump restarts for tenant 18821 (MRS1/PAR2/PAR3); WD abort alerts (LON1 x3, TPE1 OOM-kill, DFW3 x5); "Unknown_Fmt" filetype blocker (GVA1, 2 occurrences); "Vault connectivity issue detected" (LIM1/BOG1/SAO3, later DEL2/BOM2/CCU1/MAA2/HYD1); IMS resp_400 (SG-SIN2); CASB upload timeout (SIN2/Flipkart); haproxy DOWN/UP flap (DE-FRA1, rolling-deploy signature).

**Sumo/query artifacts (slice 5):** re-scoped ZMQ query `(_sourceCategory=*nsdlp_svr or _source=*dlp-core*) "ZMQ ROUTER Send Error" "ns_zmq.c" "TASK" | count by _sourceHost`; per-POP `datacenter=` value-trap discoveries (`br_sao1` only resolves via `_sourceCategory=br_sao1_dp_nsdlp_svr`; real value is `mrs1` not `fr_mrs1`; real value is `sin2` not `sg_sin2`); `--json` required to decode CCR-compressed `_raw` on DE-FRA1 haproxy log partitions; statistical co-occurrence profile-isolation method (FR-MRS1, sampled raw failing-config lines); file-size bucketing proxy query on `_index="dlp_sg_sin2_debug"` (`if(fs<1000000,"1_LT_1MB",...)` ladder) used to infer the timed-out-file size ceiling from successful-scan sizes; `cfgagentv2` config-push-timing correlation check; `curl http://dlpcfgpushervip:8887/config/refresh/<tenantid>` VIP-resolution check; 12+ ephemeral `_code=` permalinks from the EG-for-IMS saga.
